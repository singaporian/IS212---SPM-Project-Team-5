# Database configuration (used by backend)
DB_HOST=localhost
DB_PORT=55432
DB_NAME=connectsphere
DB_USER=postgres
DB_PASSWORD=postgres

# Server
PORT=3000
JWT_SECRET=replace-this-with-a-long-random-secret
